export const environment = {
  production: true,
  // Update this to your production backend URL before deployment
  // Example: 'https://your-backend.herokuapp.com/api'
  // or 'https://your-backend.railway.app/api'
  // For same-domain deployment (e.g., Vercel with proxy): '/api'
  apiUrl: "/api",
};
